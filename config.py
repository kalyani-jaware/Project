import os

# Google Cloud configuration
GOOGLE_CLOUD_BUCKET_NAME = "autotuneml_project"

# Database configuration
DATABASE_URI = "sqlite:///hyperparameters.db"  # SQLite for local DB, or use PostgreSQL, MySQL
